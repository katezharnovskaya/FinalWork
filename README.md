1. Задаем массив строк с данными.Задаем масив выборки.

2. Создаем два метода.

3. Вычисляем массив строк, где элементы <= 3. Выводим  на печать новый массив, который удовлетворяет заданному условию.
